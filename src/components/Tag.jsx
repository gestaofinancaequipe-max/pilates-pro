const Tag = ({ children }) => (
  <span className="rounded-full bg-white/70 px-3 py-1 text-xs font-medium uppercase tracking-wide text-text-muted shadow-soft">
    {children}
  </span>
);

export default Tag;

